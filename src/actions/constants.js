/**
 * A List of all the different actions
 * the application can create.
 */

// Modals
export const showModal = 'SHOW_MODAL';
export const hideModal = 'HIDE_MODAL';

// Inputs
export const inputEntered = 'INPUT_ENTERED';
